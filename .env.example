# STK MCP Server — Environment Configuration
# Copy this file to .env and modify as needed.

# STK Connect TCP host (default: localhost)
# Set to the IP address of the machine running STK if remote.
STK_HOST=localhost

# STK Connect TCP port (default: 5001)
# Must match the port configured in STK → Edit → Preferences → Connect.
STK_PORT=5001
